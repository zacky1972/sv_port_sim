# Examples

- `simple_counter/` - a minimal SystemVerilog counter with signal metadata and
  a deterministic interactive-wrapper session fixture covering reset, poke,
  cycle, peek, finish probe, and stop.
